class Unit {
  constructor(damage, pv) {
    this.age_ = 0;
    this.damage_ = damage || 0;
    this.pv_ = pv || 1;
    // This.worldEvents_ = new EventEmitter();
    // This.timeFactor_ = timeFactor || 1000;
  }
}
